name: devops-project
description: A minimal AWS Python Pulumi program
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: .env
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
